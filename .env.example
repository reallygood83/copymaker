# =============================================================================
# Not_GPT Environment Variables Template
# =============================================================================
# 🔒 보안 주의사항:
# 1. 이 파일(.env.example)은 GitHub에 커밋되는 템플릿입니다.
# 2. 실제 값은 .env 파일에 작성하세요 (.env는 .gitignore에 의해 제외됨)
# 3. Vercel 배포 시: Vercel Dashboard > Settings > Environment Variables에 입력
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI Configuration (Backend - Vercel Serverless Function)
# -----------------------------------------------------------------------------
# OpenAI API Key를 입력하세요
# 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# 사용할 OpenAI 모델 (기본: gpt-4o)
OPENAI_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# Firebase Configuration (Frontend - Client-side)
# -----------------------------------------------------------------------------
# Firebase 프로젝트 설정: https://console.firebase.google.com/
# Project Settings > General > Your apps > Web app

# Firebase API Key (공개 가능, 하지만 Firebase Console에서 도메인 제한 설정 필수)
VITE_FIREBASE_API_KEY=your-firebase-api-key

# Firebase Auth Domain (예: your-project.firebaseapp.com)
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Firebase Project ID (예: your-project-id)
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket (예: your-project.appspot.com)
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Firebase Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012

# Firebase App ID
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# Firebase Measurement ID (Google Analytics, 선택사항)
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Server Configuration (개발 환경용)
# -----------------------------------------------------------------------------
# 서버 호스트 (로컬: 0.0.0.0, 프로덕션: Vercel이 자동 관리)
HOST=0.0.0.0

# 서버 포트 (로컬 개발용, Vercel에서는 무시됨)
PORT=8000

# 디버그 모드 (개발: true, 프로덕션: false)
DEBUG=true

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# 텍스트 변환 기본 강도 (0.0 ~ 1.0)
DEFAULT_INTENSITY=0.5

# 최대 텍스트 길이 (글자 수)
MAX_TEXT_LENGTH=10000

# -----------------------------------------------------------------------------
# Special Access Code (접근 제어)
# -----------------------------------------------------------------------------
# 특별 코드 (앱 사용을 위한 비밀번호)
SPECIAL_CODE=verygood2025

# =============================================================================
# Vercel 배포 시 환경 변수 설정 방법
# =============================================================================
# 1. Vercel Dashboard 접속: https://vercel.com/dashboard
# 2. 프로젝트 선택 > Settings > Environment Variables
# 3. 위 변수들을 하나씩 추가:
#    - Variable Name: OPENAI_API_KEY
#    - Value: sk-실제-api-키
#    - Environment: Production, Preview, Development (모두 선택)
# 4. "Add" 버튼 클릭하여 저장
# 5. 모든 환경 변수 추가 후 프로젝트 재배포
# =============================================================================
